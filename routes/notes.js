// var router = require('express').Router();
// const { requiresAuth } = require('express-openid-connect');
// const database = require('../database')

// router.get('/', requiresAuh(), function (req, res, next) {
//     const userId = eq.oidc.user.id

//     database.getNotesForUser(userId, (error, notes) => {
//         if (error) {
//             //handle error
//             return 
//         }
//           res.render('notes', {
//             notes: notes,
//             title: 'Profile page'
//         });
//     })


// });

// module.exports = router;
